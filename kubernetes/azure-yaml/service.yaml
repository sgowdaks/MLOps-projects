apiVersion: v1
kind: Service
metadata:
  name: ml-model-service
spec:
  type: LoadBalancer # Tells Azure to spin up a public IP
  selector:
    app: ml-model
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000